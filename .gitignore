/target
/dist
/Cargo.lock
*.swp
*.swo
*~
.DS_Store
/.idea
/.vscode
*.iml
/tests/fixtures/*.sock
/tests/fixtures/agent-*
/tmp/
/tests/

# mcp-publisher cache (./d registry)
/.cache/

# Local machine / dev artifacts (do not commit)
/hosts
/r
/d
/d.example
/deploy.env
.deploy.env
.deploy.env.example
.mcp.json
*.local
.env
.env.*

# Deploy / release tooling (local only — not open source)
/scripts/
/homebrew/
/.github/workflows/release.yml
/.github/workflows/publish-mcp-registry.yml
/server.json

# Local brokre runtime data (if copied into repo for testing)
.brokre/
**/.brokre/

# macOS file-backed keychain keys (prevent accidental commit)
.master_kek
.audit_hmac
# Cursor IDE local config (never commit)
.cursor/
.claude/
.kimi/
bin/brokre
